[build]
  base = "frontend"
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Proxy API calls to Render backend
[[redirects]]
  from = "/api/*"
  to = "https://joaoreidosbugs-r4jo.onrender.com/api/:splat"
  status = 200
  force = true
